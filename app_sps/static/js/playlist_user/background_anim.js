
    var animation = lottie.loadAnimation({
        container: document.getElementById('playlist-background-animation'), // Контейнер для анимации
        renderer: 'svg', // SVG рендер
        loop: true, // Зацикливание
        autoplay: true, // Автоматическое воспроизведение
        path: '/static/animation_icon/bk_anm.json' // Путь к JSON файлу
    });
